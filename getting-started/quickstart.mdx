---
title: "Quickstart"
description: "Install Scott, sync your recent agent work, and share planspaces with your team."
---

## Get a quick overview

<CardGroup cols={2}>
  <Card title="How Scott AI works" icon="compass" href="/getting-started/concepts">
    Planspaces, context flows, and an agent-centric project lifecycle.
  </Card>

  <Card title="The Scott Method" icon="book-open" href="https://tryscott.ai/method">
    Our philosophy on building with agents — goal setting before agent delegation.
  </Card>
</CardGroup>

## Start with Sync

`scott sync` is the fastest way to onboard. It uploads your recent local coding-agent sessions from the last 14 days, opens the Sync Chat view, and starts an analysis of the work Scott can now see.

```bash
curl -fsSL https://tryscott.ai/install | sh
scott sync
```

After sync finishes, open the URL printed by the CLI. Scott will take you to **For You → Sync**, where you can ask across your recent Claude Code, Codex, Cursor, OpenCode, Gemini, and other supported agent sessions in one place.

<Tip>
  Run `scott sync` from your development machine, not from a hosted browser agent. The command reads local agent history before uploading the relevant context to Scott.
</Tip>

### Add code context

Sync Chat works immediately from session history. For code-aware answers, connect GitHub from the Sync Chat source picker or the planspace composer.

1. Click **Connect repo**
2. Install the Scott GitHub App for the GitHub account or organization you want Scott to read from
3. Choose which repositories the GitHub App can access
4. Return to Scott, then select the repository and branch from the repo picker

Scott lists only repositories available through your linked GitHub App installation. A repository is used as context only after you select it.

## Install Options

<AccordionGroup>

  <Accordion title="Browser use (recommended for PMs & designers)" icon="globe">
    Go to [app.tryscott.ai](https://app.tryscott.ai/login) and sign in with Google or GitHub. The browser app uses cloud sandbox execution to make it easier to collaborate with developers on their ideas.
  </Accordion>

  <Accordion title="Use Scott from your coding agent" icon="terminal">
    Run the following command to install the `scott` terminal integration. The installer wires Scott to any detected coding agents and opens a browser window for sign-in.

    ```bash
    curl -fsSL https://tryscott.ai/install | sh
    ```

    Then run the primary onboarding command:

    ```bash
    scott sync
    ```

    To turn one active coding-agent thread into a planspace for team review, say something like:

    ```bash
    # In Claude Code, Codex, OpenCode, Cursor etc.
    "push this conversation to Scott"
    ```

    Your agent will prepare the context in a form optimized for sharing, then run `scott push`. Scott prints a planspace URL you can share with teammates.

    Once your team is sufficiently aligned, you can pull the context back to a new session with all relevant team feedback:

    ```bash
    # In Claude Code, Codex, OpenCode, Cursor etc.
    "pull the project requirements from this planspace and start building -- https://app.tryscott.ai/workspace/xxxxxxx-8f6c-yyyyyy-9056-07d8ca0-zzzzz"
    ```

    Under the hood, Scott uses skills, an MCP, and the CLI to integrate with your agent and the UI.
    If the installer cannot detect your coding agent automatically, you can manually complete the setup with this command:

    ```bash
    scott setup --claude   # or --cursor, --codex, --opencode, --gemini
    ```

    See the full [CLI reference](/developer-tools/cli).
  </Accordion>

</AccordionGroup>



## Set up Scott AI

<AccordionGroup>
  <Accordion title="I'm setting up for my team" icon="users">
    1. **Install Scott** — see [Start with Sync](#start-with-sync) above.
    2. **Run `scott sync`** — bring recent local agent sessions into Scott so the team has high-quality context on day one.
    3. **Use Sync Chat** — ask what has been happening, which threads matter, and which sessions should become planspaces.
    4. **Connect GitHub when code matters** — install the Scott GitHub App, select repos, and attach the relevant branch.
    5. **Push a focused thread** — run `scott push` when one session should become a sharing document for teammates.
    6. **Invite your team to review** — use comments and [reviews](/collaboration/reviews) to converge on the plan.
    7. **Pull for execution** — run `scott pull <planspace-url>` from a fresh agent thread when it is time to build with the approved context.

    <Tip>
      Start by syncing real work. The best first experience is asking Scott about the actual agent sessions your team already ran.
    </Tip>

    **Next steps:** [Sharing](/collaboration/sharing) · [Reviews](/collaboration/reviews) · [Making the most of Scott AI](/getting-started/making-the-most)
  </Accordion>

  <Accordion title="I'm evaluating Scott AI" icon="eye">
    1. **Read The Scott Method** at [tryscott.ai/method](https://tryscott.ai/method)
    2. **Install the CLI and run `scott sync`** so Scott can analyze real local agent history.
    3. **Connect GitHub for code-aware answers** by installing the Scott GitHub App and selecting the repo branches you want Scott to use.
    4. **Push one session for review** with `scott push`, then share the planspace URL with teammates.
    5. **Pull the reviewed context into execution** with `scott pull <planspace-url>`.
    6. **See how intent links to code** — [Scott CI](/developer-tools/scott-ci) auto-links PRs back to approved specs.

    **Next steps:** [For You](/collaboration/notifications) · [Scott CI](/developer-tools/scott-ci) · [Making the most of Scott AI](/getting-started/making-the-most)
  </Accordion>
</AccordionGroup>
